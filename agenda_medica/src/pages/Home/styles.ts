import { StyleSheet } from "react-native"
export const style = StyleSheet.create({
    homeInitial:{
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: 'center',
        alignItems: 'center',
    }
})
